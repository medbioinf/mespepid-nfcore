{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/mspepident/master/assets/schema_input.json",
    "title": "nf-core/mspepident pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "id": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "id must be unique and cannot contain spaces",
                "meta": ["id"]
            },
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "sample must be provided and cannot contain spaces",
                "meta": ["sample"]
            },
            "filepath": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.(mzML|raw|RAW|d)$",
                "errorMessage": "Filepath must be provided, cannot contain spaces and must have extension '.mzML', '.raw', '.RAW', or '.d'"
            }
        },
        "required": ["id", "sample", "filepath"]
    }
}
